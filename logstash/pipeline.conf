input {
  kafka {
    bootstrap_servers => "kafka-server:9092"
    topics => "coronavirus"
    group_id => "coronavirus_consumer_group"
    auto_offset_reset => earliest
    codec => "json"
  }
}

filter {
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "coronavirus-%{+YYYY.MM.dd}"
  }
}
